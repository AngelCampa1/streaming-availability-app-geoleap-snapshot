// Playwright test file - should be run with 'npx playwright test' not Jest
// This file is intentionally disabled to prevent Jest execution conflicts

export {}; // Empty export to prevent Jest from running this file

// Placeholder test to satisfy Jest's requirement for at least one test
describe('SocialOAuthE2ETests', () => {
  it.skip('should be run with Playwright, not Jest', () => {
    // This test is intentionally skipped - run 'npx playwright test' for E2E tests
  });
});
