# EAS Ignore file for CNG (Continuous Native Generation) workflow
# These native folders will be regenerated during EAS Build using prebuild
# This allows EAS Build to sync app.config.js properties like icon, splash, etc.

# Native project folders (regenerated by prebuild)
/ios
/android

# Build artifacts and caches
/node_modules
/.expo
/dist
/build
*.log
